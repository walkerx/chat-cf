{
	"name": "chat-cf",
	"type": "module",
	"scripts": {
		"dev": "pnpm run build:frontend && wrangler dev",
		"dev:frontend": "vite",
		"build:frontend": "vite build",
		"build": "pnpm run build:frontend && wrangler publish --dry-run && echo '✓ Build successful'",
		"deploy": "pnpm run build:frontend && wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"type-check": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"pre-commit": "pnpm run cf-typegen && pnpm run type-check"
	},
	"dependencies": {
		"@types/react": "^19.2.5",
		"@types/react-dom": "^19.2.3",
		"hono": "^4.10.5",
		"react": "^19.2.0",
		"react-dom": "^19.2.0"
	},
	"devDependencies": {
		"@types/node": "^20.18.0",
		"@vitejs/plugin-react": "^5.1.1",
		"typescript": "^5.9.0",
		"vite": "^7.2.2",
		"vitest": "^2.1.0",
		"wrangler": "^4.48.0"
	}
}