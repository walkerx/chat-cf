{
	"name": "chat-cf",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler publish --dry-run && echo '✓ Build successful'",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"hono": "^4.10.5"
	},
	"devDependencies": {
		"@types/node": "^20.18.0",
		"typescript": "^5.9.0",
		"wrangler": "^4.48.0",
		"vitest": "^2.1.0"
	}
}